[project]
name = "nooa-harvey"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nooa>=0.0.8",
    "python-dotenv>=1.2.2",
]

[project.optional-dependencies]
optimize = [
    "gepa>=0.1.4",
]
